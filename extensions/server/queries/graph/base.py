"""Base class for graph queries"""
from optorch.storage.queries.base import BaseQuery


class NodeGraphBase(BaseQuery):
    """Base for graph queries - read-only node registry and execution flow queries"""
    pass
